import 'package:flutter/material.dart';

class Pallete {
  static const Color mainFontColor = Color.fromRGBO(19, 61, 95, 1);

  // Brighter button & background shades
  static const Color firstSuggestionBoxColor = Color.fromRGBO(135, 220, 240, 1); // brighter cyan-blue
  static const Color secondSuggestionBoxColor = Color.fromRGBO(130, 190, 235, 1); // richer blue
  static const Color thirdSuggestionBoxColor = Color.fromRGBO(135, 240, 241, 1); // light turquoise

  static const Color assistantCircleColor = Color.fromRGBO(190, 235, 249, 1); // lighter background
  static const Color borderColor = Color.fromRGBO(200, 200, 200, 1);

  static const Color blackColor = Colors.black;
  static const Color whiteColor = Colors.white;
}
